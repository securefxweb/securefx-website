import Affiliate from "@/rendering/affiliate";
import React from "react";

export const metadata = {
	title: "Afflilate - SecureFX",
	description: "Standard trading account with competitive spreads",
};

export default function page() {
	return (
		<div>
			<Affiliate />
		</div>
	);
}
